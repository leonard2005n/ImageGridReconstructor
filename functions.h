//315CA-Neacsa Leonard
#include "image.h"

//Function that creates grid based on the image
matrix * int_grid(image * pic, int n);

//Function that resize the image
image *resize(image *pic);

//Function that initialize the patterns
image *init_contur(int n);

//Function that applies the march algorithm
image *march(image *pic);
